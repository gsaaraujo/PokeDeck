const DATABASE_NAME = '@pokedeck';

export const USER = `${DATABASE_NAME}:user`;
export const ACCESS_TOKEN = `${DATABASE_NAME}:access_token`;
export const POKEMON_CARDS = `${DATABASE_NAME}:pokemon_cards`;
